/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  proseWrap: "always",
  trailingComma: "all",
  singleQuote: false,
  tabWidth: 2,
  semi: true,
  useTabs: false,
};

export default config;
